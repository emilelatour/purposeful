% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/purposeful-glm.R
\name{purposeful_glm}
\alias{purposeful_glm}
\title{Purposeful selection for generalised linear models}
\usage{
purposeful_glm(
  data,
  outcome,
  candidate_vars,
  keep_in_mod = NULL,
  family = stats::binomial(),
  p_screen = 0.25,
  p_remove = 0.05,
  confound_method = c("pct_change", "abs_change", "stabilized"),
  confound_threshold = NULL,
  target_var = NULL,
  small_coef_warn = TRUE,
  confound_pct = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble containing all variables.}

\item{outcome}{Character string.  The left-hand side of the model formula
(the outcome / dependent variable).}

\item{candidate_vars}{Character vector.  Variables to consider as
predictors.}

\item{keep_in_mod}{Character vector or \code{NULL}.  Variables with strong
clinical or theoretical reasons to remain in the model regardless of
their p-value.  They are included after univariable screening and are
never removed during backward elimination.  Default is \code{NULL}.}

\item{family}{A \code{family} object passed to \code{\link[stats]{glm}}.
Default is \code{binomial()} for logistic regression.  Exponentiation of
final coefficients is applied automatically for \code{binomial()} models
(producing odds ratios) and suppressed for all other families.}

\item{p_screen}{Numeric between 0 and 1.  Variables with a univariable
p-value below this threshold pass screening.  Hosmer and Lemeshow suggest
0.25.  Default is \code{0.25}.}

\item{p_remove}{Numeric between 0 and 1.  Variables with a multivariable
drop1() p-value at or above this threshold are candidates for removal.
Hosmer and Lemeshow suggest 0.05.  Default is \code{0.05}.}

\item{confound_method}{Character.  Which metric to use when assessing
whether removing a candidate variable would change coefficients enough to
call it a confounder.  One of:
\describe{
\item{\code{"pct_change"}}{Textbook Hosmer-Lemeshow: \eqn{|\beta_{new} -
    \beta_{old}| / |\beta_{old}|}.  Misbehaves when any coefficient is
small in absolute terms; tiny shifts amplify into large percentages
and over-retain confounders.  This is the default for backward
compatibility but a warning is emitted when small coefficients are
detected.}
\item{\code{"abs_change"}}{Absolute change on the model's natural (log)
scale: \eqn{|\beta_{new} - \beta_{old}|}.  A 0.10 threshold
corresponds to roughly a 1.10-fold change in the OR/HR.}
\item{\code{"stabilized"}}{SE-scaled change: \eqn{|\beta_{new} -
    \beta_{old}| / SE(\beta_{old})}.  Robust to small
coefficients by construction; a noisy coefficient needs more absolute
movement to look like one SE of change.}
}
Default is \code{"pct_change"}.}

\item{confound_threshold}{Numeric or \code{NULL}.  Threshold above which
\code{confound_method}'s metric triggers confounder retention.  If
\code{NULL} (the default), a method-aware default is used: \code{0.20}
for \code{"pct_change"}, \code{0.10} for \code{"abs_change"}, \code{1.00}
for \code{"stabilized"}.}

\item{target_var}{Character or \code{NULL}.  When supplied, the confounding
check is computed only on coefficients of this variable, instead of all
remaining coefficients.  Use this when you have a single exposure of
scientific interest and you only care whether adjusting changes that
variable's coefficient.  Combine with any \code{confound_method}.
Default is \code{NULL}.}

\item{small_coef_warn}{Logical.  When \code{confound_method = "pct_change"},
emit a one-time warning at the start of Phase 2 if any coefficient in
the initial multivariable model has \eqn{|\beta| < 2 \cdot SE}
(a regime where pct_change is unreliable).  Default \code{TRUE}.}

\item{confound_pct}{(deprecated) Use \code{confound_threshold} instead.
If supplied, its value is forwarded to \code{confound_threshold}.}

\item{verbose}{Logical.  Print a step-by-step log of the selection
procedure.  Default is \code{TRUE}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{uni_screen}}{Tibble of univariable screening results (one row
per candidate variable, sorted by p-value).}
\item{\code{screened_vars}}{Character vector of variables that passed
univariable screening (including any \code{keep_in_mod} variables).}
\item{\code{history}}{Tibble describing every step of the backward
elimination, including the action taken (\code{"remove"},
\code{"retain_confounder"}, or a \code{"stop_*"} reason), the p-value
of the worst variable at that step, the metric used
(\code{confound_metric}), and the value of that metric at the worst-
aggregating coefficient (\code{max_change}).}
\item{\code{final_vars}}{Character vector of variables in the final model.}
\item{\code{final_fit}}{The fitted \code{glm} object.}
\item{\code{final_term_tests}}{Tibble of variable-level drop1() p-values
for the final model.}
\item{\code{final_tidy}}{Tidied coefficient table from \code{broom::tidy()},
with confidence intervals.  Exponentiated for \code{binomial()} models.}
}
}
\description{
Applies the purposeful selection procedure of Hosmer and Lemeshow to a
generalised linear model.  The procedure has three phases:

\enumerate{
\item \strong{Univariable screening.}  Each candidate variable is fitted
in a separate model.  Variables with a variable-level p-value
(from \code{drop1()}) below \code{p_screen} are carried forward.
Variables in \code{keep_in_mod} are always carried forward.
\item \strong{Backward elimination with integrated confounding check.}
Starting from all screened variables, the variable with the largest
drop1() p-value is identified at each step.  If that p-value is
\eqn{\ge} \code{p_remove}, removal is attempted.  Before the
variable is actually dropped, the change in remaining coefficients
is assessed using \code{confound_method}; if the change exceeds
\code{confound_threshold}, the variable is retained as a confounder
and protected from future removal, and the algorithm continues
evaluating the next candidate for removal.
\item \strong{Final model assembly.}  The surviving model is fitted and
tidied.
}

Variable-level p-values from \code{drop1()} are used throughout (not
coefficient-level Wald p-values).  This is especially important for
multi-level factors, where a single variable-level test is needed for
screening and removal decisions.
}
\examples{
library(dplyr)

# Simulated data -----------------------------------------------------------
set.seed(888)
n <- 1000
age    <- abs(round(rnorm(n, 67, 14)))
lac    <- abs(round(rnorm(n,  5,  3), 1))
gender <- factor(rbinom(n, 1, 0.6), labels = c("male", "female"))
wbc    <- abs(round(rnorm(n, 10,  3), 1))
hb     <- abs(round(rnorm(n, 120, 40)))
z      <- 0.1 * age - 0.02 * hb + lac - 10
pr     <- 1 / (1 + exp(-z))
mort   <- factor(rbinom(n, 1, pr), labels = c("alive", "dead"))
dat    <- tibble::tibble(age, gender, lac, wbc, hb, mort)

# Basic logistic regression ------------------------------------------------
res <- purposeful_glm(
  data           = dat,
  outcome        = "mort",
  candidate_vars = c("age", "gender", "lac", "wbc", "hb"),
  family         = binomial(),
  p_screen       = 0.25,
  p_remove       = 0.05,
  verbose        = TRUE
)

res$uni_screen        # univariable screening table
res$history           # step-by-step log
res$final_vars        # variables in the final model
res$final_term_tests  # drop1() p-values for the final model
res$final_tidy        # tidied coefficients (odds ratios for binomial)

# With a forced variable ---------------------------------------------------
res2 <- purposeful_glm(
  data           = dat,
  outcome        = "mort",
  candidate_vars = c("age", "gender", "lac", "wbc"),
  keep_in_mod    = "hb",
  verbose        = FALSE
)

res2$final_vars
}
\references{
Hosmer DW, Lemeshow S (2000) \emph{Applied Logistic Regression}.
John Wiley & Sons, Inc.
}
