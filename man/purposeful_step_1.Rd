% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated-steps.R
\name{purposeful_step_1}
\alias{purposeful_step_1}
\title{Purposeful selection step 1 (deprecated)}
\usage{
purposeful_step_1(
  data,
  outcome,
  predictors,
  ref_level = NULL,
  cutoff_value = 0.25,
  conf_level = 0.95,
  format = FALSE,
  exponentiate = TRUE,
  digits = 1,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing all variables.}

\item{outcome}{Character string.  The left-hand side of the model formula
(the outcome / dependent variable).}

\item{predictors}{Character vector of candidate predictors.}

\item{ref_level}{Character string. Factor level of the outcome that
corresponds to the event (1). If \code{NULL} (default) the model
determines the reference level.}

\item{cutoff_value}{Numeric. Univariable screening threshold passed to
\code{p_screen}. Default \code{0.25}.}

\item{conf_level}{Confidence level for intervals. Default \code{0.95}.}

\item{format}{Logical. Format results for display. Default \code{FALSE}.}

\item{exponentiate}{Logical. Exponentiate estimates. Default \code{TRUE}.}

\item{digits}{Integer. Decimal places to round to. Default \code{1}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A tibble of univariable results.
}
\description{
Deprecated. Please use \code{\link{purposeful_glm}} instead, which runs
all three steps in a single call and also supports Cox and linear
regression.
}
\keyword{internal}
